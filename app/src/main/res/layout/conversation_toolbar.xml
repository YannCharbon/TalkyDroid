<?xml version="1.0" encoding="utf-8"?>
<Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="60dp"
    android:layout_width="match_parent"
    android:background="@color/backgroundLauncherColor"
    android:orientation="horizontal"
    android:titleTextColor="@android:color/white">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageButton
            android:id="@+id/conversation_toolbar_back_arrow_imageButton"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_baseline_arrow_back"
            android:background="@null"
            android:layout_marginEnd="5dp"
            android:layout_centerVertical="true"/>

        <ImageView
            android:id="@+id/conversation_toolbar_avatar_imageView"
            android:layout_toEndOf="@+id/conversation_toolbar_back_arrow_imageButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="0dp"
            android:layout_margin="1dp"
            android:background="@drawable/ic_circle_avatar"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_baseline_unknown_user"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintHorizontal_bias="0"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/conversation_toolbar_title_textView"
            android:layout_toEndOf="@+id/conversation_toolbar_avatar_imageView"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_centerVertical="true"
            android:gravity="center"
            android:layout_marginStart="10dp"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:text="@string/unknown">

        </TextView>

        <ImageButton
            android:id="@+id/conversation_toolbar_settings_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="5dp"
            android:background="@null"
            android:src="@drawable/ic_baseline_settings">

        </ImageButton>

    </RelativeLayout>


    
</Toolbar>
    
    