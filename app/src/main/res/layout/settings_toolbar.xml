<?xml version="1.0" encoding="utf-8"?>
<Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="60dp"
    android:layout_width="match_parent"
    android:background="@color/backgroundLauncherColor"
    android:orientation="horizontal"
    android:titleTextColor="@android:color/white">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageButton
            android:id="@+id/settings_toolbar_back_arrow_imageButton"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_baseline_arrow_back"
            android:background="@null"
            android:layout_marginEnd="5dp"
            android:layout_centerVertical="true"
            >

        </ImageButton>

        <TextView
            android:id="@+id/conversation_toolbar_title_textView"
            android:layout_toEndOf="@+id/settings_toolbar_back_arrow_imageButton"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_centerVertical="true"
            android:gravity="center"
            android:layout_marginStart="10dp"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:text="@string/settings">
        </TextView>

    </RelativeLayout>

</Toolbar>

